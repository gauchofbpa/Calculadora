var primeiroNumero = 0;
var segundoNumero = 0;
var resultado = 0
function soma() {
    soma() === true;
}
function calcular() {
    if (soma() === true) {
        resultado = primeiroNumero + segundoNumero;
    }
    alert("seu resultado foi: " + resultado);
}